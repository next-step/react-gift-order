import styled from '@emotion/styled';

export const MessageImage = styled.img`
    display: inline-block;
    margin: 5px;
    border-radius: 10px;
    height: 60px;

    &:hover {
        cursor: pointer;
    }
`
