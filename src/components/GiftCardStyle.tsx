import theme from "@/styles/theme"
import styled from "@emotion/styled"

const GiftCardStyle = styled.div`
  img {
    width: auto;
    height: 56px;
    border-radius: 0.5rem;
    border: 3px solid ${theme.colors.gray900};
  }
`

export default GiftCardStyle
